#include "holberton.h"
/**
 * rot13 - encodes a string into rot13
 *@string: the string
 *Return: return 0
 */
char *_strchr(char *s, char c)
{
  while (*s != '\0')
    {
      if (*s == c)
	return (s);
      else if (!*s)
	return ('\0');
      s++;
	  }
  return (s);
}
