#include <stdio.h>
#include <stdlib.h>
/**
 * main - return the name
 *@argc: argc
 *@argv: argv
 *Return: return 0
 */
int main(int argc, char *argv[])
{
  (void)argv;
  printf("%d\n", argc);
  return (0);
}
