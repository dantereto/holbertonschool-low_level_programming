#include "holberton.h"
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
/**
 * strtow - concatet two strings
 *@str: the string
 *Return: reyturn 0
 */
char **strtow(char *str)
{
if (str == NULL || str == 0)
return ('\0');
return (0);
}
